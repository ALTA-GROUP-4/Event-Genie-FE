import { FC } from "react";
import { Layout } from "../components/Layout";
const Payment: FC = () => {
  return (
    <Layout>
      <div>
        <p>Payment</p>
      </div>
    </Layout>
  );
};

export default Payment;

import { FC } from "react";
import { Layout } from "../../components/Layout";
const Login: FC = () => {
  return (
    <Layout>
      <div>
        <p>Login</p>
      </div>
    </Layout>
  );
};

export default Login;
